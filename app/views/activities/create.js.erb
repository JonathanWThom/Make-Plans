<% @activity.errors.keys.each do |key| %>
  var input = $(".activity_" + "<%= key %>");
  input.addClass("error");
  input.find("input").focus(function() {
    input.removeClass("error")
  })
<% end %>

// errors for bad locations
